# SonarCloud code quality and security analysis.
name: SonarCloud

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    # Same MongoDB service as the Go CI "test" job, so the coverage profile
    # Sonar sees includes the MongoDB-backed stores instead of skipping them.
    services:
      mongodb:
        image: mongo:7
        ports: ["27017:27017"]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod

      - name: Generate Go coverage
        # Produce a coverage profile for Sonar. Test correctness is gated by the
        # dedicated "test" job (Go CI workflow); here we only need the profile,
        # so a flaky/failing test must not abort the analysis. The coverage file
        # is still written for the packages whose tests ran.
        env:
          TEST_MONGODB: "1"
        run: go test -race -covermode=atomic -coverprofile=coverage.out ./... || true

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v8
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=${{ github.repository_owner }}
            -Dsonar.projectKey=${{ github.repository_owner }}_${{ github.event.repository.name }}
            -Dsonar.go.coverage.reportPaths=coverage.out
